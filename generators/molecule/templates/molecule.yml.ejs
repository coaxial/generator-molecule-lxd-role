---
dependency:
  name: galaxy
driver:
  name: lxd
lint:
  name: yamllint
<%- platforms %>
provisioner:
  name: ansible
  playbooks:
    create: ../create.yml
<% if (playbookMode) { -%>
    converge: ../../playbook.yml
<% } -%>
    destroy: ../destroy.yml
  lint:
    name: ansible-lint
scenario:
  name: default
verifier:
  name: testinfra
  lint:
    name: flake8
